= render 'form'

- if @results
  - @results.each do |result|
    = link_to result.is_a?(User) ? result.name : result.try(:title) || result.try(:body).truncate(100), polymorphic_path(result)

p#no-results  No results found.
