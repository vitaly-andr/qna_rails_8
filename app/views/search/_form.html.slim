= form_with url: search_path, method: :get, local: true, data: { controller: "autocomplete" } do
  = text_field_tag :query, params[:query], placeholder: "Search...", data: { autocomplete_target: "input", action: "input->autocomplete#search" }
  div data-autocomplete-target="results"
